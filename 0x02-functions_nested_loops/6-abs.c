#include "main.h"
/**
 * _abs - Computes the abs value of an int
 * @n: the int in the question
 * Return: the abs of the int.
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
